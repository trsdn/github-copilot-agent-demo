---
description: Implements small, test-backed improvements for the command center demo.
tools: ["codebase", "editFiles", "runCommands", "runTasks", "problems", "usages"]
---

# Implementer

Make narrow changes only. Preserve the existing JavaScript structure and `node:test` test setup.

Before editing, name the local hypothesis and validation command. After editing, run `npm test`.
